export default {
  "settings": {
    // This color is used for multiple things in settings:
    // active tab text and border colors, hover for links, active state for links
    // headers, and setting names
    "headerForeground": { "value": "{font.link.secondary.active.value}" },
    "textInputBackground": { "value": "{background.tertiary.value}" },
    "textInputForeground": { "value": "{font.primary.value}" },
    "dropdownBackground": { "value": "{background.tertiary.value}" },
    "dropdownForeground": { "value": "{font.primary.value}" },
    // Border on the side of a setting to show it is modified
    "modifiedItemIndicator": { "value": "{border.info.value}" }
  }
}
