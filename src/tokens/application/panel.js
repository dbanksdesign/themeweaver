export default {
  // The bottom thing with the terminal, debug console, problems, and output
  "panel": {
    "border": { "value": "{border.tertiary.value}" },
    "background": { "value": "{background.tertiary.value}" },
    "dropBackground": { "value": "{background.drop.value}" }
  },
  
  "panelTitle": {
    "activeBorder": { "value": "{border.active.value}" },
    "activeForeground": { "value": "{font.primary.value}" },
    "inactiveForeground": { "value": "{font.secondary.value}" }
  }
}
