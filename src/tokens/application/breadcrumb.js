// Breadcrumbs are between the editor and the tabs
export default {
	"breadcrumb": {
		"background": { "value": "{background.primary.value}" },
		"foreground": { "value": "{font.secondary.value}" },
		"focusForeground": { "value": "{font.link.primary.inactive.value}" },
		"activeSelectionForeground": { "value": "{font.link.primary.active.value}" }
	}
}
