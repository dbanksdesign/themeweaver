// The minimap shows the code overview on the right side of the editor
// Enable it in settings
export default {
  "minimap": {
    "findMatchHighlight": { "value": "{application.editor.findMatchBackground.value}" },
    "selectionHighlight": { "value": "{application.editor.selectionBackground.value}" }
  }
}
